<% if message.similar_chunks.any? %>
  <div class="space-y-2">
    <% message.similar_chunks.each_with_index do |chunk, index| %>
      <%= link_to chunk, class: "block group", target: "_blank", rel: "noopener" do %>
        <div class="flex items-start gap-3 p-3 rounded-lg bg-neutral-50 dark:bg-neutral-900/50 border border-neutral-200 dark:border-neutral-700 hover:border-blue-400 dark:hover:border-blue-500 transition-all hover:shadow-sm">
          <!-- Numéro de la source -->
          <div class="flex-shrink-0 w-5 h-5 rounded-full bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-400 flex items-center justify-center text-xs font-semibold">
            <%= index + 1 %>
          </div>

          <!-- Contenu de la source -->
          <div class="flex-1 min-w-0">
            <div class="text-sm font-medium text-neutral-900 dark:text-neutral-100 group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors line-clamp-2">
              <%= chunk.title %>
            </div>
            <% if chunk.respond_to?(:document) && chunk.document.present? %>
              <div class="mt-1 text-xs text-neutral-500 dark:text-neutral-400 flex items-center gap-1">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
                </svg>
                <span class="truncate"><%= chunk.document.name rescue 'Document' %></span>
              </div>
            <% end %>
          </div>

          <!-- Icône de lien externe -->
          <div class="flex-shrink-0">
            <svg class="w-4 h-4 text-neutral-400 group-hover:text-blue-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
            </svg>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
<% end %>
