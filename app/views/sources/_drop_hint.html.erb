<%# Persistent drag-and-drop affordance. The whole sources list is a dropzone
    (see index.html.erb), so this hint is shown both when the list has rows and
    in the empty states. %>
<p class="flex items-center justify-center gap-1.5 border-b border-dashed n-main-border px-2 py-2 text-xs text-neutral-400">
  <span aria-hidden="true">⬇︎</span> Drop files anywhere to add documents
</p>
