<div class="p-4">
  <%= link_to new_chat_path, class: "n-btn-group border n-main-border n-main-hover n-main-focus" do %>
    <%= inline_svg "svg/plus.svg", title:"New chat", class:"h-5 w-5" %>
    New Chat
  <% end %>
</div>
<div class="space-y-2 h-[calc(100vh-6.45rem)] lg:h-[calc(100vh-9.4rem)] overflow-y-auto ">
  <% Current.user.chats.root.order(updated_at: :desc).each do |chat| %>
    <div class="grid grid-cols-6 gap-2 items-center mx-4 first:mt-2 last:mb-4">
      <%= link_to chat, class: "col-span-5 w-full flex flex-col gap-y-2 rounded-lg px-3 py-2 n-main-hover n-main-focus" do %>
        <h1 class="text-sm font-medium text-neutral-700 dark:text-neutral-200 truncate">
          <%= chat.title %>
        </h1>
        <p class="text-xs text-neutral-500 dark:text-neutral-400"><%= l(chat.updated_at, format: :short) %></p>
      <% end %>
      <%= button_to chat, method: :delete, data: { turbo_confirm: "Are you sure?" }, class:"text-red-600 dark:text-red-300 n-btn-icon n-main-hover-danger n-main-focus-danger" do %>
        <%= inline_svg "svg/trash-bin.svg", title:"Delete chat", class:"h-5 w-5" %>
      <% end %>
    </div>
  <% end %>
</div>
